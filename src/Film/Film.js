import React from 'react';

const Film = ({ filmData }) => {
console.log(filmData.results)
const filmText = Object.keys(filmData).map((key, i) => {
})

  return(
    <div>test</div>
  )
}

export default Film;
